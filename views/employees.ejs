<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee portal</title>
</head>
<body>
    <%- include('partials/header')%>
    <h1>Employee Details</h1>
    <table align="center" border="5">
        <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>ID</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            <% allEmployeesDetails.forEach(emp => { %>
                <tr>
                    <td><%= emp.first_name %></td>
                    <td><%= emp.last_name %></td>
                    <td><%= emp.email %></td>
                    <td>
                        <a href="http://localhost:8000/web/employees/update?eid=<%= emp._id %>">Update</a>
                      </td>                      
                    <td>    <a href="http://localhost:8000/web/employees/delete?eid=<%=emp._id %>">Delete</a>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
    <%- include('partials/footer')%>
</body>
</html>